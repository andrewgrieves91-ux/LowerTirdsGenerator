node_modules/
*.log
.DS_Store
state.json
dist/mac-arm64/
dist/*.zip
dist/*.dmg
dist/*.blockmap
dist/builder-debug.yml
.electron-cache/
.dmg-staging/
releases/*.zip
releases/*.dmg
.lt-companion-cues.json
.lt-companion-grid-layout.json
.lt-companion-grid-size.json
.archive/
# Personal recordings / screen captures should not live in the repo.
*.mov
# Vitest snapshot updates are in tests/__snapshots__ and should be tracked,
# but Vitest itself caches state under tests/.vitest-cache.
tests/.vitest-cache/
# DeckLink native addon: the hand-rolled DeckLinkAPI.h + DeckLinkAPIDispatch.cpp
# in electron/native/decklink-output/include/ ARE committed (they are our own
# minimal interface, not the redistribution-restricted Blackmagic SDK). Only
# the build artefacts under build/ are generated.
electron/native/decklink-output/build/
